# Suppress ESLint warnings in terminal (the === vs == noise)
ESLINT_NO_DEV_ERRORS=true

# Don't try to open browser from inside Docker
BROWSER=none

# Suppress source map generation (not useful inside container)
GENERATE_SOURCEMAP=false
